# Anthropic API key for /api/chat concierge
ANTHROPIC_API_KEY=

# Admin panel (/admin)
ADMIN_PASSWORD=change-me
SESSION_SECRET=generate-a-long-random-string-at-least-32-chars

# Vercel Blob — auto-injected by Vercel when Blob is enabled on the project.
# Needed at runtime for the admin panel to persist project edits.
BLOB_READ_WRITE_TOKEN=
